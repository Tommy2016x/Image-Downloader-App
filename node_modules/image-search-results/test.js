'use strict';

let search = require('./index');

search.googleInt({query: 'cat', count: 200})
  .then((res) => {
    console.log(JSON.stringify(res, 0 , 2));
  })
  .catch((err) => {
    console.log('ERR', err);
  });

'use strict';

let google = require('./google');
let googleInt = require('./google-int');

module.exports = (params) => {
  if ( params.query > 1000 ){
    return googleInt(params);
  } else {
    return google(params);
  }
};

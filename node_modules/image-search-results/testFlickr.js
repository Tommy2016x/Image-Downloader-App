'use strict';

let search = require('./index');

search.flickr({query: 'cat', count: 2000})
  .then((res) => {
    console.log(JSON.stringify(res));
    console.log(res.length);
  })
  .catch((err) => {
    console.log('ERR', err);
  });
